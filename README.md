# google_flight_search
Python script that scrapes Google Flights to monitor and analyze hundreds of flight combinations (routes, dates, durations). It extracts prices, durations and stops, and turns noisy search data into structured insights to optimize travel decisions based on real data.
